{
  "//": "复制到 ~/.flowx/agents.json（机器级）或本仓 .flowx/agents.json（项目级覆盖）。真跑前需要它。",
  "//cursor": "cursor-default 用本机已登录的 cursor-agent，无需 provider/key，最省事。",
  "//byo": "recursive/aider/claude 才能注入 provider；需配合 ~/.flowx/providers.json + 对应 API key 环境变量。",
  "agents": {
    "cursor-default": { "executor": "cursor", "model": "auto" },
    "claude-sonnet": { "executor": "claude", "model": "claude-sonnet-4" }
  }
}
